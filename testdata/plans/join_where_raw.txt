FILTER [(col("k") == col("k_right")) AND (col("lv") < col("rv"))]
  JOIN CROSS
    left:
      MEMORY SCAN 5 rows
        projection: * (2 cols)
    right:
      MEMORY SCAN 4 rows
        projection: * (2 cols)
